{% extends "ShopMainBundle:Admin:layout.html.twig" %}

{% block body %}

    <div class="admin-container">
        {{ include("ShopAdminBundle:Default:header.html.twig") }}
        <div class="row dashboard">
            <ul class="four_up tiles">
                {{ include("ShopAdminBundle:Default:menuItems.html.twig") }}
            </ul>
        </div>
    </div>

{% endblock %}
