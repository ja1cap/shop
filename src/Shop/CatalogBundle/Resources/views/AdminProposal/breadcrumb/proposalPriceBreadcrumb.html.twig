{% extends 'ShopCatalogBundle:AdminProposal:breadcrumb/proposalPricesBreadcrumb.html.twig' %}
{% block breadcrumb_items %}
    {{ parent() }}
    <li class="medium btn default">
        <a href="{{ path('proposal_price', {'proposalId' : proposal.id, 'id' : price.id}) }}">
            {{ title }}
        </a>
    </li>
{% endblock %}