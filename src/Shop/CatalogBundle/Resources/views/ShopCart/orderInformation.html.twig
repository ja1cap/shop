<h3>
    Сумма заказа: {{ shopCartSummary.summaryPrice|weasty_price }}
</h3>
<table>
    <thead>
        <tr>
            <th>
                Товар
            </th>
            <th>
                Сумма
            </th>
        </tr>
    </thead>
    <tbody>
    {% for categoryData in shopCartSummary.categories %}
        {% for proposalData in categoryData.proposals %}
            <tr>
                <td>
                    {{ categoryData.category.singularName|capitalize }} - {{ proposalData.proposal.title }}
                </td>
                <td>
                    {% for priceData in proposalData.prices %}
                        <div>
                            {{ priceData.summary|weasty_price }} x {{ priceData.amount }} = {{ priceData.summary|weasty_price }}
                            <div>
                                {{ priceData.price.description }}
                            </div>
                        </div>
                        <br/>
                    {% endfor %}
                </td>
            </tr>
            {% endfor %}
    {% endfor %}
    </tbody>
</table>
{% if action %}
    <h4>
        Вы выбрали подарок
    </h4>
    <div>
        {{ action.title }}
    </div>
    <br/>
    <div>
        {{ action.description|nl2br }}
    </div>
    <br/>
{% endif %}